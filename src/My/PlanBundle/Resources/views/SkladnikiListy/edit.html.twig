<h1>SkladnikiListy edit</h1>

<form action="{{ path('skladnikilisty_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('skladnikilisty') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('skladnikilisty_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
