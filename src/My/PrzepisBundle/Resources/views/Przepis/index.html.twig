<h1>Przepis list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nazwa</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('przepis_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nazwa }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('przepis_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('przepis_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('przepis_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
