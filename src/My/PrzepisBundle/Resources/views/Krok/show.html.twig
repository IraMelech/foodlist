<h1>Krok</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Opis</th>
            <td>{{ entity.opis }}</td>
        </tr>
        <tr>
            <th>Czas</th>
            <td>{{ entity.czas }}</td>
        </tr>
        <tr>
            <th>Przepis_id</th>
            <td>{{ entity.przepisid }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('krok') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('krok_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('krok_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
