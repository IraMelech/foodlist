<h1>Krok creation</h1>

<form action="{{ path('krok_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('krok') }}">
            Back to the list
        </a>
    </li>
</ul>
