<h1>Krok list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Opis</th>
            <th>Czas</th>
            <th>Przepis_id</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('krok_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.opis }}</td>
            <td>{{ entity.czas }}</td>
            <td>{{ entity.przepisid }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('krok_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('krok_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('krok_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
